
infile = '_img/image.jpg';

I = imread(infile);

A = ones(3,3);

Y = conv2(I,A);

J = zeros(2387,2387
for i = 1:3
    
    J = J + I(1:2387,:,i);
    
end

figure(1);
image(I);

figure(2);
image(Y);
